<div class="page">
  <!-- HEADER -->
  <div style="
          display: flex;
          justify-content: space-between;
          align-items: center;
          padding: 20px 60px;
          font-size: 18px;
          color: #1e3a8a;
        ">
    <span>🌐 www.costsegregationguys.com</span>
    <span>Page 06</span>
  </div>

  <!-- CONTENT -->
  <div style="padding: 10px 60px 40px; color: #000;">

    <!-- Project Timeline Section -->
    <h2 style="
            font-size: 36px;
            color: #0f2e5a;
            margin: 0 0 16px 0;
            line-height: 1.25;
            text-transform: uppercase;
            letter-spacing: 0.5px;
            ">
      <%= proposalData.page6.content.timeline.title %>
    </h2>
    <p style="font-size: 24px; margin-bottom: 30px; line-height: 1.5; color: #334155;">
      <%= proposalData.page6.content.timeline.text %>
    </p>

    <!-- Fees Section -->
    <h2 style="
            font-size: 24px;
             color: #0f2e5a;
            margin: 0 0 16px 0;
            line-height: 1.25;
             text-transform: uppercase;
             letter-spacing: 0.5px;
            ">
      <%= proposalData.page6.content.fees.title %>
    </h2>
    <p style="font-size: 24px; margin-bottom: 20px; line-height: 1.5; color: #1e3a8a; font-weight: bold;">
      The Fee For The Study Is <%= proposalData.page6.content.fees.amount %>
    </p>

    <!-- Terms Section -->
    <% proposalData.page6.content.terms.forEach(term=> { %>
    <p style="font-size: 24px; margin-bottom: 12px; line-height: 1.4; color: #334155;">
      <%= term %>
    </p>
    <% }) %>

  </div>

  <!-- ANGLED SHAPES -->
  <div style="
            position: absolute;
            width: 80px;
            height: 280px;
            background: #0f2e5a;
            transform: rotate(45deg);
            right: -70px;
            bottom: 100px;
            z-index: -1;
        "></div>
  <div style="
            position: absolute;
            width: 80px;
            height: 280px;
            background: #1e4ed8;
            transform: rotate(45deg);
            right: -80px;
            bottom: 40px;
            z-index: -2;
        "></div>
</div>